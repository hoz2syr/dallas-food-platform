# Example environment variables for local development

# Service ports
PORT=3000

# Database connection (postgres)
DATABASE_URL=postgres://dallas:dallas@localhost:5432/dallas

# API key used to authenticate requests (replace with your own in real env)
API_KEY=changeme
