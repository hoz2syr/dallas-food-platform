// WebSocketClient مبسط (استبدله بتكامل فعلي مع backend)
export class WebSocketClient {
  static connect() {
    // مثال: استخدم مكتبة socket.io-client أو WebSocket API
    // هنا فقط واجهة وهمية
    return {
      on: (event: string, callback: Function) => {},
      disconnect: () => {}
    };
  }
}
