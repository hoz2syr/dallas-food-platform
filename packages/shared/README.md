# packages/shared

Purpose: This package will contain shared utilities, types, and libraries used across services. At present it's a placeholder; add focused, well-documented shared modules here to avoid duplication and promote consistency across services.
